# Contributing to Pulse CLI

1. Fork and create a feature branch.
2. Add tests for the behavior you change.
3. Run `pytest` (or the project test command) before opening a PR.
4. Keep the README and `/docs` in sync with public APIs.
